# 个人计划表

一个轻量的个人计划管理表，形态接近飞书表格，并带 ChatAI。

功能：

- 表格化维护任务：状态、优先级、计划日期、项目、标签、备注。
- ChatAI 会读取当前任务表，帮你做计划、复盘、优先级判断。
- AI 根据自然语言目标生成任务行，可追加到表格。
- SQLite 持久化。

服务端需要环境变量：

- `VENUS_API_URL`
- `VENUS_API_KEY`
- `VENUS_MODEL`
- `DATA_DIR`
